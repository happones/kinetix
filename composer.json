{
    "name": "happones/kinetix",
    "description": "A modern Vue 3 and Inertia.js dashboard and admin toolkit for Laravel applications.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "happones",
            "email": "happones@hotmail.com"
        }
    ],
    "require": {
        "php": "^8.3",
        "illuminate/support": "^11.0|^12.0|^13.0",
        "inertiajs/inertia-laravel": "^2.0|^3.0",
        "spatie/laravel-data": "^4.0",
        "phpoffice/phpspreadsheet": "^5.8"
    },
    "require-dev": {
        "orchestra/testbench": "^9.0|^10.0|^11.0",
        "spatie/laravel-typescript-transformer": "^2.0",
        "larastan/larastan": "^3.0",
        "laravel/cashier": "^15.0|^16.0",
        "laravel/pint": "^1.0",
        "spatie/laravel-permission": "^6.0",
        "spatie/laravel-activitylog": "^4.8",
        "laravel/pennant": "^1.0",
        "laravel/scout": "^10.0",
        "spatie/laravel-webhook-server": "^3.0",
        "laravel/sanctum": "^4.0",
        "laravel/socialite": "^5.0"
    },
    "suggest": {
        "spatie/laravel-permission": "Roles & permissions for the Kinetix permissions feature (^6.0). Required only if you enable kinetix.permissions.",
        "spatie/laravel-activitylog": "Preferred driver for the Kinetix activity feature (^4.8). When installed, Kinetix logs through it instead of its native store.",
        "laravel/pennant": "Preferred driver for the Kinetix feature-flags feature (^1.0). When installed, Kinetix resolves flags through Pennant; otherwise a native evaluator is used.",
        "laravel/scout": "Search driver for the Kinetix spotlight command palette (^10.0). When a searched model is Searchable, spotlight routes through Scout; otherwise it uses a LIKE query.",
        "spatie/laravel-webhook-server": "Preferred delivery driver for the Kinetix webhooks feature (^3.0). When installed, deliveries route through its tuned retries/backoff; otherwise Kinetix's native queued job is used.",
        "laravel/sanctum": "Required for the Kinetix developer-tokens feature (^4.0). The User model must use Laravel\\Sanctum\\HasApiTokens.",
        "laravel/socialite": "Required for the Kinetix connected-accounts / social-auth feature (^5.0). Configure providers in config/services.php and kinetix.connected_accounts.providers."
    },
    "autoload": {
        "psr-4": {
            "Happones\\Kinetix\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Happones\\Kinetix\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse",
        "format": "pint"
    },
    "extra": {
        "laravel": {
            "providers": [
                "Happones\\Kinetix\\KinetixServiceProvider"
            ]
        }
    }
}
